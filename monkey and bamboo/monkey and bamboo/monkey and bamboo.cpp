// monkey and bamboo.cpp : Questo file contiene la funzione 'main', in cui inizia e termina l'esecuzione del programma.
//

#include <iostream>
#include <stdlib.h>
#include <vector>
using namespace std;

//k Strength factor (max jump height) if I jump exactly k feet in a jump, k is decremented by 1.
int findMinK(vector<int> &ladder) {

}
int calcRemaining() {}
int exponentialSearch(int (*f)(), ) {
}

int main()
{
    int T = 0; //#test cases
    cin >> T;
    for (int i = 0; i < T; i++) {
        int n = 0; //#rungs
        cin >> n;
        vector<int> ladder(n); //rungs distance from ground
        for (int j = 0; j < n; j++) {
            cin >> ladder[j];
        }
    }
}


